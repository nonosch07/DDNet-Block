#ifndef BLOCK_VERSION_H
#define BLOCK_VERSION_H

// Block mod version, kept out of upstream's src/game/version.h so that
// upstream's release bumps never conflict with ours.
#define BLOCK_VERSION "1.7"

#endif // BLOCK_VERSION_H
